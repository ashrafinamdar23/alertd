# ---- OS cruft ----
.DS_Store
Thumbs.db
ehthumbs.db
Icon?
desktop.ini

# ---- IDE / Editor ----
.vs/
.idea/
*.suo
*.user
*.userosscache
*.sln.docstates
*.userprefs
*.csproj.user
*.vcxproj.user
*.DotSettings.user
.history/
.vscode/**/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json

# ---- .NET build outputs ----
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/
*.pidb
*.svclog
*.cache/
*.dbmdl
*.jfm
**/TestResults/
BenchmarkDotNet.Artifacts/

# ---- Publish / artifacts ----
**/artifacts/
**/publish/
**/out/

# ---- NuGet packages (keep spec files, ignore produced packages) ----
*.nupkg
*.snupkg
# If you manually author nuspec files, comment out the next line
# *.nuspec

# ---- Entity Framework Core ----
# Keep Migrations in source control. Nothing to ignore here intentionally.

# ---- Local configuration & secrets (keep examples) ----
appsettings.*.Local.json
appsettings.Local.json
*.secrets.json
*.secret.json
# If you create env files for local runs:
.env
.env.local
.env.*.local
# (Do commit .env.example if you have one)

# ---- SQLite or local DB files (in case of samples/tools) ----
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# ---- Node / Front-end tooling (if used by RCL or site assets) ----
node_modules/
.pnpm-store/
.turbo/
.eslintcache
.parcel-cache/
.npmrc
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
dist/
build/
# Next/Nuxt/Vite caches (if ever used)
.next/
.nuxt/
.vite/

# ---- wwwroot build artifacts (keep if you vendor libs manually) ----
# If you generate bundles into wwwroot, ignore those folders:
wwwroot/**/dist/
wwwroot/**/build/
wwwroot/**/bundles/
wwwroot/**/assets/
# Source maps from bundlers (uncomment if you don't want to commit them)
# wwwroot/**/*.map

# ---- Coverage / test outputs ----
coverage/
*.coverage
*.coveragexml
coverage*.json
coverage*.xml
lcov.info
*.trx
TestResult*.xml

# ---- Docker / Compose (optional overrides) ----
.docker/
docker-compose.*.override.yml
docker-compose.override.local.yml

# ---- Misc ----
*.swp
*.swo
*.orig
*.tmp
